// Branding asset slots (favicon, logos) arrive with the phase 3 asset
// pipeline — undefined keeps every consumer on its current fallback.
export const getO2dFaviconUrl = (): string | undefined => undefined;
