// O2D-PATCH: P6 — default workspace name reads the distribution fallback
import { getO2dProductName } from '@/o2d-branding/utils/getO2dProductName';

export const DEFAULT_WORKSPACE_NAME = getO2dProductName();
